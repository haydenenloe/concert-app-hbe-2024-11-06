<html>
  <head>
    <style>
      /* General Styles */
      body {
        font-family: 'Arial', sans-serif;
        background-color: #f9f9f9;
        margin: 0;
        padding: 0;
        text-align: center;
        color: #333;
      }

      h1 {
        color: #ff6f00;
        font-size: 36px;
        margin: 20px 0;
      }

      p {
        font-size: 18px;
        margin-bottom: 30px;
      }

      /* Table Styles */
      table {
        width: 80%;
        margin: 0 auto;
        border-collapse: collapse;
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        background-color: white;
        border-radius: 10px;
        overflow: hidden;
      }

      th, td {
        padding: 15px;
        text-align: left;
      }

      th {
        background-color: #ff6f00;
        color: white;
        font-size: 18px;
        text-transform: uppercase;
      }

      td {
        font-size: 16px;
        border-bottom: 1px solid #ddd;
      }

      tr:hover {
        background-color: #ffe6cc;
      }

      /* Donut-Specific Styles */
      .guava-lava {
        color: #ff4500; /* Bright reddish-orange for Guava Lava */
        font-family: 'Comic Sans MS', cursive; /* Fun, playful font */
      }

      .red-apple-fritter {
        color: #b22222; /* Deep red for Red Apple Fritter */
        font-family: 'Georgia', serif; /* Classy serif font */
      }

      .maple-seaside {
        color: #8b4513; /* Maple syrup brown */
        font-family: 'Courier New', monospace; /* Monospaced font for a unique look */
      }

      .coconut-boston-cream {
        color: #6a5acd; /* Rich purple for Coconut Boston Cream */
        font-family: 'Verdana', sans-serif; /* Clean, modern font */
      }
    </style>
  </head>
  <body>
    <h1>Paradise Concerts</h1>
    <p>Here are the concert locations and their featured donuts!</p>
    
    <table>
      <thead>
        <tr>
          <th>Concert Location</th>
          <th>Featured Donut</th>
        </tr>
      </thead>
      <tbody>
        <% for (let iCount = 0; iCount < aConcerts.length; iCount++) { %>
        <tr>
          <td><%= aConcerts[iCount].location %></td>
          <td class="<%= aConcerts[iCount].featured_donut.toLowerCase().replace(/\s+/g, '-') %>">
            <%= aConcerts[iCount].featured_donut %>
          </td>
        </tr>
        <% } %>
      </tbody>
    </table>

    <footer>
      &copy; 2024 Paradise Concerts. All rights reserved.
    </footer>
  </body>
</html>
